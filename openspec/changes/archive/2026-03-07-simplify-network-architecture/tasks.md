## 1. 网络模型层
- [x] 1.1 修改 `neural-network.js` 默认网络结构为 [2, 2, 1]，移除偏置初始化
- [x] 1.2 修改前向传播为纯线性计算（去除激活函数调用）
- [x] 1.3 修改反向传播，去除 Sigmoid 导数项，使用纯线性偏导
- [x] 1.4 修改损失函数为单输出平方误差 Loss = ½(y - target)²
- [x] 1.5 更新 `createDefaultNetwork()` 的初始权重 w₁~w₆ 和默认输入/目标值

## 2. 视图渲染层
- [x] 2.1 更新 `network-renderer.js` 绘制 5 个节点（x₁, x₂, h₁, h₂, y）和 6 条连线
- [x] 2.2 移除偏置节点和 E_total 菱形节点的绘制逻辑
- [x] 2.3 更新 `formula-display.js` 所有子步骤的公式展示（去除 Sigmoid 公式）
- [x] 2.4 更新动画引擎适配新的 2-2-1 网络结构

## 3. 控制与交互层
- [x] 3.1 修改 `index.html`：移除激活函数选择器，更新学习率默认值
- [x] 3.2 修改 `ui-controller.js`：移除激活函数切换逻辑
- [x] 3.3 更新训练信息面板：移除激活函数显示行

## 4. 教育页面
- [x] 4.1 重写 `education.js`：以 2-2-1 无激活无偏置网络为例，逐步演示完整 BP 流程
- [x] 4.2 更新侧边栏导航适配新的章节结构

## 5. 验证
- [x] 5.1 验证前向传播数值正确性
- [x] 5.2 验证反向传播梯度正确性
- [x] 5.3 验证权重更新后误差下降
- [x] 5.4 验证 `vite build` 构建成功
- [x] 5.5 验证页面可视化和交互功能正常
