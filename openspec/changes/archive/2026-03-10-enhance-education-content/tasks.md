## 1. 链式法则展开公式
- [x] 1.1 在 `src/pages/education.js` 的「简化公式」小节中，「偏导公式」之后、「梯度下降公式」之前，新增 `<h4>链式法则（反向传播核心）</h4>` 子标题
- [x] 1.2 添加输出层权重的链式法则展开公式：∂E/∂w₅ = ∂E/∂y · ∂y/∂w₅ = (y − target) · h₁，使用 `renderFormula()` 渲染
- [x] 1.3 添加隐藏层权重的链式法则展开公式：∂E/∂w₁ = ∂E/∂y · ∂y/∂h₁ · ∂h₁/∂w₁ = (y − target) · w₅ · x₁，使用 `renderFormula()` 渲染

## 2. 训练收敛对比小节
- [x] 2.1 在 `src/pages/education.js` 中，「步骤五：权重更新」(`#sec-update`) 之后、「关键概念」(`#sec-concepts`) 之前，插入 `<h3 id="sec-convergence">训练收敛对比</h3>` 标题
- [x] 2.2 在新标题下添加 `edu-step-card` 容器，包含说明文字和对比表格（`edu-data-table` 样式）
- [x] 2.3 对比表格展示初始、第 1 轮、第 10 轮、第 100 轮四个时间点的：网络输出 y、损失 Loss、以及 6 个权重值（通过模型精确计算）
- [x] 2.4 在表格后添加总结段落，强调"每轮迭代误差持续下降，输出逼近目标值 4"，并引导用户到可视化页面体验

## 3. 验证
- [x] 3.1 本地构建成功（`npx vite build`），无语法错误和 lint 错误
- [x] 3.2 链式法则公式位于偏导公式（第 55~58 行）与梯度下降公式（第 66 行）之间
- [x] 3.3 收敛对比小节（`#sec-convergence`，第 158 行）位于「步骤五」与「关键概念」之间
- [x] 3.4 侧边栏导航自动识别 `h3[id="sec-convergence"]`（`buildSidebarNav` 查询 `h3[id]`）
- [x] 3.5 收敛对比表数据通过 `node` 脚本精确计算验证（epoch 1/10/100 的 y、loss、w1~w6 均与模型实际结果一致）
